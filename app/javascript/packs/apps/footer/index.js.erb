import Vue from "vue/dist/vue.esm";
import App from "./App";

Vue.config.debug = true;

document.addEventListener("DOMContentLoaded", () => {
  const app = new Vue({
    render: h => h(App)
  }).$mount("footer");
});
